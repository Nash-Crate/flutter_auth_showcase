/// Enum representing different types of logs in the application.
enum LogType {
  /// Log type for login events.
  login,

  /// Log type for registration events.
  register,

  /// Log type for generic events.
  generic,
}
