/// Enum representing different logging methods in the application.
enum LogMethod {
  /// login to the app using email and password.
  loginWithEmailPassword,

  /// register a new user with email and password.
  registerWithEmailPassword,

  /// logout from the app.
  logout,
}
